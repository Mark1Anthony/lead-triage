[pytest]
# Tests import app.py and triage.py from the repo root, so the root has to be
# on sys.path. Without this, a plain `pytest` (as opposed to `python -m pytest`)
# fails to import them - which is exactly what CI runs.
pythonpath = .
testpaths = tests

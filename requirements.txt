fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
openai>=1.12
pydantic>=2.6

# Postgres driver. Only imported when DATABASE_URL points at Postgres,
# so a plain SQLite run does not need it at runtime.
psycopg[binary]>=3.2
